# testing
test:
    - 'eslint:source'
    
# stylesheets
stylesheets:
    - 'less:dist'

# javascripts
javascripts:
    - 'webpack:dev'

# build
build:
    - 'javascripts'
    - 'stylesheets'

# travis
deploy:
    - 'gh-pages:local'